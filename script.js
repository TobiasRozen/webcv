// Obtener la referencia al cuadrado
var cuadrado = document.getElementById("cuadrado");

// Agregar evento al pasar el mouse por encima
cuadrado.addEventListener("mouseover", function() {
});

// Agregar evento al sacar el mouse de encima
cuadrado.addEventListener("mouseout", function() {
  // Restablecer la posición original del cuadrado
  cuadrado.style.transform = "rotate(0deg)";
});

// Agregar evento al presionar el cuadrado
cuadrado.addEventListener("click", function() {
  // Redirigir a otra página
  window.location.href = "/html/info.html";
});


// Obtener la referencia al cuadrado
var cuadrado = document.getElementById("cuadrado2");

// Agregar evento al pasar el mouse por encima
cuadrado.addEventListener("mouseover", function() {
});

// Agregar evento al sacar el mouse de encima
cuadrado.addEventListener("mouseout", function() {
  // Restablecer la posición original del cuadrado
  cuadrado.style.transform = "rotate(0deg)";
});

// Agregar evento al presionar el cuadrado
cuadrado.addEventListener("click", function() {
  // Redirigir a otra página
  window.location.href = "/html/info.html";
});